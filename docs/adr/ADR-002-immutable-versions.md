# ADR-002：版本不可变与归档删除

- 日期：2026-08-09
- 状态：已接受

## 决策
- 提示词/Skill/MCP 定义的已发布版本不可修改；只能新建版本或归档。
- 删除业务记录默认归档（`archived=true`），不做物理删除。
- MinIO 对象仅在未被任何版本引用时才允许后台物理清理。

## 理由
不可变版本保证历史可回滚、可审计；归档删除避免误删仍被引用的文件。
